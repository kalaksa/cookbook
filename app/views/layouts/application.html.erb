<!DOCTYPE html>
<html>
  <%= render 'layouts/head' %>

  <body>
    <%= render 'layouts/header' %>
    <div class="container">
      <p class="flash"><%= flash.notice %></p>
      <%= yield %>
      <%= render 'layouts/footer' %>
    </div>
  </body>
</html>
